const mocha = require('mocha');
const assert = require('assert');
const ProductModel = require('../models/product');


describe('Import tests', function(done){
	//validate a record data-type
	it('Validate Schema types', function(done){
		//TODO://
	});
});